# MarketMind AI — direct dependencies (pinned to Python 3.11.9 verified install)
# Verified: clean .venv311 + pytest 172/172 on 2026-08-05
# Transitive pins live in requirements-lock.txt (audit artifact only — do not use as sole install source without review)
python-dotenv==1.2.2
requests==2.34.2
streamlit==1.61.1
anthropic==0.120.2
pandas==3.0.5
numpy==2.4.6
plotly==6.9.0
geopy==2.5.0
tenacity==9.1.4
pydantic==2.13.4
